package com.practice.mypractice.interview7;

public class CurrencyConverter {

    // Agoda 4th Round (LLD):

    public String convertCurrency(double value, Currency fromCurrency, Currency toCurrency) {
        /*
         * Pseudo Code:

        String result = "";
        double rate = currencyClient.getExchangeRate(fromCurrency, toCurrency);
        double convertedValue = rate * value;

        return String.valueOf(convertedValue);*/
        return "";
    }
}
